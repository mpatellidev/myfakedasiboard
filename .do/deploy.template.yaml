name: dasiboard
static_sites:
  - name: dasiboard-web
    github:
      repo: SEU_USUARIO/SEU_REPOSITORIO
      branch: main
      deploy_on_push: true
    build_command: npm install && npm run build
    output_dir: dist
    environment_slug: node-20
